services:
  db:
    image: postgres:16
    restart: always
    ports:
      - 15432:5432
    volumes:
      - postgres-data:/var/lib/postgresql/data
    env_file:
      - .env

volumes:
  postgres-data:
